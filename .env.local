# GraphQL MCP Configuration
PORT=3000
GRAPHQL_MCP_ENDPOINT=https://api.github.com/graphql
GRAPHQL_API_KEY=github_pat_
AUTH_TYPE=Bearer
GRAPHQL_READ_ONLY=true

# Open WebUI Configuration
# Note: Open WebUI usually runs on 8080 via Docker, but we might configure it differently locally
OPENAI_API_BASE_URL=http://localhost:11434/v1
OPENAI_API_KEY=ollama
ENABLE_OPENAI_API=true
WEBUI_SECRET_KEY=local-dev-secret-key
